
import "UIPanel"

class UITest : UIPanel {

    init(uiFile) {
        super.init("Test", uiFile)
    }

    func enter() {
    
        if self.gameObject == nil {
            self.gameObject = UIManager.Instance.LoadPanel("UI/TestPanel")

        }
         
        self.gameObject.SetActive(true)

    }

    func onLoad(go) {
        self.gameObject = go
        print("onload", self.gameObject)
        
        //print(getmetatable(self.gameObject))
        print(self.gameObject.name)
        self.gameObject.SetActive(true)
    }

    func exit() {  

        if self.gameObject != nil {  
            self.gameObject.SetActive(false)    
        }

    }

	func shutdown() {
		print "shutdown"
	}

}
