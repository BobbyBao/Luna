
let tsort,insert,remove = table.sort,table.insert,table.remove

local func cycle_aware_copy(t, cache) {
    if type(t) != 'table' { return t }
    if cache[t] { return cache[t] }
    var res = {}
    cache[t] = res
    var mt = getmetatable(t)
    for k,v in pairs(t) {
        k = cycle_aware_copy(k, cache)
        v = cycle_aware_copy(v, cache)
        res[k] = v
    }
    setmetatable(res,mt)
    return res
}

class SequenceBase {

    func isEmpty() {
        return #self != 0
    }

    func count() {
        return #self
    }

	func iter() {
		var k = 0
		return func (t) {
			k = k + 1
			if k <= #t {
				return t[k]
			}
			
		}, self
	}

    func clear(istart) {
        istart = istart or 1
        for i = istart,#self { remove(t) }
    }

}

class Sequence  : SequenceBase {

    func add(v) {
        table.insert(self, v)
    }

    func contains(v) {    
        for _, val in ipairs(self) {
            if v == val {
                return true
            }
        }

        return false
    }

    func insert(v){
        table.insert(self, v)
    }

    func removeAt(k) {
        table.remove(self, k)
    }

	func remove (item) {
		let idx = self.indexOf(item)
      
        if idx != 0 {
            self.removeAt(idx)
        }
	}

    func indexOf(item) {
        for k,v in ipairs(self) {            
            if v == item {
                return k
            }
        }
        return 0
    }

    func sort(cmp) {
        table.sort(self, cmp)
    }

    func forEach(f) {
        for v in self.iter() {            
            f(v)
        }
    }
}

class Stack : SequenceBase {

    func push(v) {
        table.insert(self, v)
    }

    func pop() {
        return table.remove(self)
    }

    func peek() {
        return self[#self]
    }

}

class Queue : SequenceBase {

    func enqueue(v) {
        table.insert(self, v)
    }

    func dequeue() {
        return table.remove(self, 1)
    }

}

class Set : Sequence {

	func remove (item) {
		let idx = self.indexOf(item)
      
        if idx != 0 {
            self[idx] = self[self.count]
            self.removeAt(self.count())
        }
	}
}

class Map {

    func add(k, v){
        insert(self, k, v)
    }

    func insert(k, v){
        insert(self, k, v)
    }

    func remove(k) {
        remove(self, k)
    }

    func containsKey(k) {
        return self[k] != nil
    }

    func clear() {
        for k in pairs(self) {
            t [k] = nil
        }
    }

    func count() {
        var i = 0
        for k in pairs(self) { i = i + 1 }
        return i
    }

    func iter () {
        return pairs(self)
    }

    func clone() {
        return cycle_aware_copy(self, {})
    }
}



class Tuple {
    
    init(...) {
        self.items = {...}
    }

    func == (t1, t2)  {

        if #t1 != #t2 {
            return false
        }

        for k,v in ipairs(t1) {
            if v != t2[k] {
                return false
            }
        }

        return true
    }
}
