import "class"

class Object {
	
	func __close() {		
	}
	
	func __tostring () {
		let mt = self._class
		let name = mt.name
		setmetatable(self,nil)
		var str = tostring(self)
		setmetatable(self,mt)
		if name { str = name ..str:gsub('table','') }
		return str
	}

}
